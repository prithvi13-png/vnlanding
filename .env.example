# Public site origin, used for canonical URLs, sitemap and OpenGraph metadata.
NEXT_PUBLIC_SITE_URL=https://www.vriddhinexus.com

# Travel service URLs. Only Bus is live today — config/services.ts hardcodes
# each service's status, so setting a URL here does not by itself activate
# a coming-soon service; the status flag in config/services.ts must also
# change to "live". A service with status "live" but no URL falls back to
# ComingSoonModal rather than rendering a broken link.
NEXT_PUBLIC_BUS_URL=https://vnbus-api.vercel.app
NEXT_PUBLIC_FLIGHT_URL=
NEXT_PUBLIC_TRAIN_URL=
NEXT_PUBLIC_CAR_URL=

# Endpoint the contact/enquiry modal POSTs to. If unset, it shows a
# "not yet configured" error state instead of faking a successful submission.
#
# Easiest free setup — pick one:
#   Formspree (formspree.io): create a form -> use its endpoint here as-is,
#     leave NEXT_PUBLIC_CONTACT_ACCESS_KEY unset.
#   Web3Forms (web3forms.com): no signup, just verify an email -> use
#     https://api.web3forms.com/submit here, and set the access key below.
NEXT_PUBLIC_CONTACT_API_URL=https://api.vriddhinexus.com/contact

# Only needed for a form-backend that identifies the destination inbox via a
# public access key in the request body instead of the endpoint URL (e.g.
# Web3Forms). Not a secret — it only routes submissions, it can't read them.
NEXT_PUBLIC_CONTACT_ACCESS_KEY=
